meta {
  name: POST climatisation stop (v1.16.1 path-suffix variant)
  type: http
  seq: 24
}

post {
  url: {{base_url}}/v2/vehicles/{{vin}}/climatisation/stop
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {}
}

docs {
  v1.16.1 (#53 Gerhard CUPRA Born) — primary climatisation stop URL.
  Action in path, empty body. Mirrors 03_POST_climatisation_start.bru.

  Implementation: cariad/api/seat_cupra.py:command_stop_climate (line ~663)
  → _post_climatisation_action(vin, "stop").

  Legacy fallback path (used on 404):
    POST /v2/vehicles/{vin}/climatisation  body {"action": "stop"}
}
